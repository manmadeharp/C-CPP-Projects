#include <stdio.h>

int main()
{

	printf("Hello World\n");
	printf("Plan on getting input too present text from the C programming language in order to help Learn C\n");
	printf("this program will be the main program in which all other programs inspired by the book are run\n");
}
// hi my name is jeff
/*
Variables and Arithmetic Expressions

Variable Printing:
%d - print as a decimal integer
%6d - print as a 6 character decimal integer
%f - print as a floatig point 
%.2f - prints to 2 sig fig
int temp
{
	float fahr, celsius;
	float lower, upper, step;
	lower = 0;
	upper = 300;
	step = 20;

	fahr = lower;
	while (fahr <= upper) {
		celsius = (5.0/9.0) * (fahr-32.0) ; 
		printf("%d\t%d\n", fahr, celsius);
		fahr = fahr + step;
	}
}
*/
/*
// For statement
// int forTemp

// Symbolic Constant
#define LOWER 0
#define UPPER 300
#define STEP 20

int forTemp()
{
	int fahr
	
	// fahr = 0 - the initialization
	// fahr<= 300 - the test if its true the for loops expressions are executed 
	// fahr = fahr + 20 - this is the increment it is executed when the expressions have been executed
	
	for (fahr = LOWER; fahr <= UPPER; fahr + STEP){
		printf("%3d %6.1f\n", fahr, (5.0/9.0)*(fahr-32.0));
		}
}
*/




